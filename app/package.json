{
    "name": "app",
    "version": "0.0.0",
    "description": "The devnet guest: @op-cartesi/app booted with the devnet genesis, plus the app-specific contracts",
    "type": "module",
    "dependencies": {
        "@op-cartesi/app": "workspace:*",
        "viem": "^2.55.10"
    },
    "devDependencies": {
        "@types/node": "^26.1.2",
        "esbuild": "^0.28.1",
        "typescript": "^7.0.2"
    },
    "scripts": {
        "build": "node esbuild.mts",
        "clean": "rm -rf node_modules && rm -rf dist",
        "typecheck": "tsc --noEmit"
    },
    "keywords": [
        "cartesi",
        "deroll"
    ],
    "trustedDependencies": [
        "esbuild"
    ]
}
