[package]
name = "cartesi-accounts-drive"
version = "0.1.0"
edition = "2021"
description = "Pure-Rust implementation of the Cartesi Accounts Drive Format v1 (no dependencies)"

# Zero third-party crates, tests included: the library builds and tests offline.
[dependencies]
