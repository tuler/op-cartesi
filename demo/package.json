{
    "name": "demo",
    "version": "0.0.0",
    "description": "The devnet application: @op-cartesi/app booted with the devnet genesis, plus the app-specific contracts",
    "type": "module",
    "dependencies": {
        "@op-cartesi/app": "workspace:*",
        "viem": "^2.55.18"
    },
    "devDependencies": {
        "@types/node": "^26.2.0",
        "esbuild": "^0.28.2",
        "typescript": "^7.0.2"
    },
    "scripts": {
        "build": "node esbuild.mts",
        "clean": "rm -rf node_modules && rm -rf dist",
        "typecheck": "tsc --noEmit"
    },
    "keywords": [
        "cartesi"
    ],
    "trustedDependencies": [
        "esbuild"
    ]
}
