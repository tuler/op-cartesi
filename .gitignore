/op-cartesi
/bin/
# `cartesi build` stores the machine snapshot here — the chain's genesis state.
/.cartesi/
*.test
jwt.hex
devnet/rollup.json
devnet/snapshot/
devnet/deployment/
devnet/l1-addresses.env
devnet/outputs-addresses.env
devnet/chain-genesis.env
# Written by the compose bring-up (devnet/compose/genesis.ts) for engine.sh.
devnet/chain-flags
devnet/token.env
node_modules/
.env
.env.local
