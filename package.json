{
    "name": "op-cartesi",
    "private": true,
    "workspaces": [
        "accounts-drive/js",
        "abi-drive/js",
        "evm-compat/js",
        "app",
        "demo",
        "devnet",
        "scripts"
    ],
    "trustedDependencies": [
        "@cartesi/rollup",
        "esbuild"
    ],
    "scripts": {
        "build": "bun run --filter demo build",
        "check": "biome check",
        "check:fix": "biome check --write",
        "format": "biome format --write",
        "lint": "biome lint",
        "test": "bun run --filter '*' test",
        "typecheck": "bun run --filter '*' typecheck"
    },
    "devDependencies": {
        "@biomejs/biome": "2.5.8"
    }
}
