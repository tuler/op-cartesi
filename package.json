{
    "name": "op-cartesi",
    "private": true,
    "workspaces": [
        "lib/accounts-drive/js",
        "lib/abi-drive/js",
        "lib/evm-compat/js",
        "guest/ts",
        "demo",
        "devnet",
        "scripts"
    ],
    "trustedDependencies": [
        "@cartesi/rollup",
        "esbuild"
    ],
    "scripts": {
        "build": "bun run --filter demo build",
        "check": "biome check",
        "check:fix": "biome check --write",
        "format": "biome format --write",
        "lint": "biome lint",
        "test": "bun run --filter '*' test",
        "typecheck": "bun run --filter '*' typecheck"
    },
    "devDependencies": {
        "@biomejs/biome": "2.5.8"
    }
}
